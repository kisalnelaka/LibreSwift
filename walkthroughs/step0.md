# Step 0: Automated Setup Engine

Install required system dependencies (`libimobiledevice`, `usbmuxd`) and download the LibreSwift toolchains (`xtool`, `rcodesign`) automatically with a single click.
